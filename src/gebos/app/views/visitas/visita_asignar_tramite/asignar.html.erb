<div class="marco-contenido">

  <div class="marco-titulo"><div><div>
	Asignando Técnico de Campo al Trámite
</div></div></div>

    <%= form_remote_tag(
        :loading =>'image_load()',
        :loaded => 'image_unload()',
        :url => { :action => :save_asignar, :id=>@solicitud.id } ) -%>
      <%= render :partial => 'form' %>
      <%= form_buttons_edit %>
    <%= end_form_tag -%>
    <%= error %>
</div>