<%= form_remote_tag(
  :loading =>'image_load()',
  :loaded => 'image_unload()',
  :url => { :action => :save_new, :solicitud_id => @solicitud.id } ) do -%>

  <%= render :partial => 'form' %>

<% end -%>