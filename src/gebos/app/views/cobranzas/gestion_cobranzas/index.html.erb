

<%= form_title_new %>

<div class="marco-contenido2">

  <%= error %>

  <%= message %>

  <%= form_remote_tag(
    :loading =>'image_load()',
    :loaded => 'image_unload()',
    :url => { :action => :save_new, :id=>@prestamo.id }) do -%>
      <%= render 'form' %>
  <% end -%>
</div>


