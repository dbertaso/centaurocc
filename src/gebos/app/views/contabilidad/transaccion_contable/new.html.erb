<%= form_title_new %>
		
<%= render :partial=>'contabilidad/transaccion_contable/common/tabs' %>
<div class='marco-contenido2'>

	<%= error %>
  <%= message %>

	<%= form_remote_tag(
		:loading =>'image_load()',
		:loaded => 'image_unload()',
	  :url => { :action => :save_new } ) do -%>
    </br>
		<%= render 'form' %>
    </br>
		<%= form_buttons_edit %>
	<% end -%>
</div>
