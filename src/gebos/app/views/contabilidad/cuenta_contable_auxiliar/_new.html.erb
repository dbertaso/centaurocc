<%= form_title_new %>

<%= error 'form_new' %>

<%= form_remote_tag(
	:loading =>'image_load()',
	:loaded => 'image_unload()',
  :url => {:cuenta_contable_id => @cuenta_contable.id, :action => :save_new } ) -%>
		<%= render :partial => 'form' -%>
			<%= link_to_remote(
        image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
			  :loading =>'image_load()',
				:loaded => 'image_unload()',
        :url => { :action => :cancel_new} ) -%>
			</td>
    </tr>
  </table>
<%= end_form_tag -%>