<div class="marco-contenido">

  <%= form_title_new %>

  <%= render :partial=>'clientes/cliente_empresa/common/tabs' %>
  <div class="marco-contenido2">

    <%= error %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_new, :id=>@empresa.id, :popup=>params[:popup] } ) do -%>
    <%= render :partial => 'form' %>
    <%= form_buttons_edit :popup=>params[:popup] %>
    <% end -%>
  </div>

</div>