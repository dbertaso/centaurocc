<div class="marco-titulo"><div><div>
      Modificando <%= Etiquetas.etiqueta(9) %> Jurídico <span class="titulo_modificar">'<%= @empresa.alias %>'</span>
    </div></div></div>

<%= render :partial=>'clientes/cliente_empresa/common/tabs' %>
<div class="marco-contenido2">

  <%= form_title_edit @empresa_integrante.persona.nombre_corto %>

  <%= render :partial=>'clientes/cliente_empresa_integrante_persona/common/tabs' %>
  <div class="marco-contenido2">
    <%= error  %>
    <%= message  %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :popup=>params[:popup], :empresa_id => @empresa.id, :id => @empresa_integrante_tipo.id } ) do -%>

    <%= render :partial => 'form' %>

    <div id="buttons_edit" style="display: none">
      <%= form_buttons_edit :empresa_id => @empresa.id, :popup=>params[:popup] %>
    </div>

    <%= link_to( image_tag(I18n.t('Sistema.Body.Imagenes.cerrar')),
      { :action => :index, :popup=>params[:popup], :empresa_id => @empresa.id },
      { :id=>'button_close', } ) -%>

    <% end -%>
  </div>
</div>