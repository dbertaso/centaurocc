<% errors_for @empresa_integrante_tipo unless @empresa_integrante_tipo.nil? %>
<%= errors_for @empresa_integrante unless @empresa_integrante.nil? %>
<%= errors_for @persona unless @persona.nil? %>
<%= errors_for @cliente unless @cliente.nil? %>