<%#= form_title_new %>
<div class="marco-titulo">
  <div>
    <div>
      Agregando Desvio de Silo <span class="titulo_modificar"></span> &ensp;
      <label style="color: red">"Nro. Tr&aacute;mite <%= @view_boleta_arrime.nro_solicitud unless @view_boleta_arrime.nil? %>" &ensp;
        "Productor  <%= @view_boleta_arrime.productor unless @view_boleta_arrime.nil? %>"</label>
    </div>
  </div>
</div>

<%= error %>
<%= message %>

<%= form_remote_tag(
  :loading =>'image_load()', :loaded => 'image_unload()',
  :url => { :action => :save_new, :solicitud_id =>params[:solicitud_id], :rubro_id=>params[:rubro_id], :cliente_id=>params[:cliente_id], :popup=>params[:popup] } ) do -%>

<%= render :partial => 'form' %>

<%= form_buttons_edit :solicitud_id =>params[:solicitud_id], :popup=>params[:popup] %>

<% end %>
