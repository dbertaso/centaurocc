<div class="marco-titulo">
  <div>
    <div>
      Modificando Desvio de Silo <span class="titulo_modificar"></span> &ensp;
      <label style="color: red">"Nro. Tr&aacute;mite <%= @view_boleta_arrime.nro_solicitud unless @view_boleta_arrime.nil? %>" &ensp;
        "Productor  <%= @view_boleta_arrime.productor unless @view_boleta_arrime.nil? %>"</label>
    </div>
  </div>
</div>

<%= error  %>
<%= message  %>

<%= form_remote_tag(
  :loading =>'image_load()',
  :loaded => 'image_unload()',
  :url => { :action => :save_edit, :id => @desvio_silo.id, :solicitud_id =>2, :popup=>params[:popup] } ) do -%>

  <%= render :partial => 'form' %>

  <div id="buttons_edit" style="display: none">
    <%= form_buttons_edit  :solicitud_id =>@desvio_silo.solicitud_id, :popup=>params[:popup] %>
  </div>

  <%= link_to(image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
    { :controller=> 'desvio_silo', :action => :index, :solicitud_id =>@desvio_silo.solicitud_id, },  { :id=>'button_close' } ) -%>

<% end %>
