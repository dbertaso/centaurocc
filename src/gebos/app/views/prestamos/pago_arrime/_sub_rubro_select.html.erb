<%= select(
  'sub_rubro_id', '',
 options_from_collection_for_select(@sub_rubro_list, 'id', 'nombre'),
      {:prompt=>I18n.t('Sistema.Body.Vistas.General.seleccione')},
      { :class=>'select',  :onchange=>"#{remote_function(
                    :loading =>'image_load()',
                    :loaded => 'image_unload()',
                    :method => :get,
                    :url=>{:controller=>'pago_arrime',:action=>:sub_rubro_change },
                    :with=>"'sub_rubro_id='+ this.value")};inputChange();"}) %> 
