           <%rubro_onchange = remote_function(
            :loading =>'image_load()',
            :loaded => 'image_unload()',
            :method=>:get,
            :url=>{:controller=>'orden_despacho',
            :action=>:rubro_change },
            :with=>"'id='+value") %>

<%= select(
  'rubro_id', '',
  @rubro_list.map{|c| [c.nombre, c.id]},
 {:prompt=> I18n.t('Sistema.Body.Vistas.Form.seleccionar')},
 { :class=>'select', :onchange=>rubro_onchange + ';inputChange();' } ) %>
