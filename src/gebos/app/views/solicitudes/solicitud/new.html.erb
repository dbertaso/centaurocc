<div class="marco-contenido">

  <%= form_title_new %>

  <%= render :partial=>'solicitudes/solicitud/common/tabs' %>
  <div class="marco-contenido2">

    <%= error %>

    <%= form_remote_tag(
        :loading =>'image_load()',
        :loaded => 'image_unload()',
        :url => { :action => :save_new, :controlador => 'solicitud', :cliente_id=>@cliente.id } ) do -%>
      <%= render :partial => 'form' %>
      <%= form_buttons_edit %>
    <% end -%>
  </div>

</div>