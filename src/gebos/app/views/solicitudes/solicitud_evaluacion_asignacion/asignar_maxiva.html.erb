<div class="marco-contenido">

  <div class="marco-titulo"><div><div>
	Asignando masivamente Técnico de Campo al Trámite
</div></div></div>

    <%= form_remote_tag(
        :loading =>'image_load()',
        :loaded => 'image_unload()',
        :url => { :action => :save_asignar_maxivo, :id=>@solicitud.id } ) -%>
      <%= render :partial => 'form_maxivo' %>
      <%= form_buttons_edit %>
    <%= end_form_tag -%>
    <%= error %>
</div>