<div class="marco-titulo"><div><div>
	Modificando <%= Etiquetas.etiqueta(9) %> Jurídico <span class="titulo_modificar">'<%= @empresa.alias %>'</span>
</div></div></div>


<div class="marco-contenido2">

	<div class="marco-boton-in" id="button_new">
		<%= link_to(image_tag(I18n.t('Sistema.Body.Imagenes.agregar')), 
		:action => 'new', :popup=>params[:popup], :empresa_id => @empresa.id ) if vlink 'edit' %>
	</div>
	
	<%= error %>
	<%= message %>

	<div id="list">
		<%= render :partial=>"list" %>
	</div>
	
	<%= link_to( image_tag(I18n.t('Sistema.Body.Imagenes.cerrar')),
		{ :controller => 'cliente_empresa', :action => :index, :popup=>params[:popup] },
		{ :id=>'button_close' } ) -%>
	
</div>