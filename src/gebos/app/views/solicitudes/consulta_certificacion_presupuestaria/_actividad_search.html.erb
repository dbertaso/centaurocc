<%= select(
  'actividad_id', '',
  @actividad_list.map{|e| [e.nombre, e.id]},
  { :prompt => I18n.t('Sistema.Body.Vistas.Form.seleccionar')},
  { :class=>'select', :onchange=>'inputChange(); cList();document.getElementById(\'errorExplanation\').style.display = \'none\';' } ) %>
