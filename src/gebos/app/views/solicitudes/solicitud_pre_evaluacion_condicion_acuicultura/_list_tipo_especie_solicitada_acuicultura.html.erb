

<% lista = 'lista0'
@tipo_especie_solicitada_acuicultura.each { |i| %>
  &nbsp;&nbsp;
  <%= check_box 'recaudo_check',
  i.id,
  {:checked=>i.marcado, :onchange=>"#{remote_function(
    :loading =>'Element.show("loading")',
    :loaded => 'Element.hide("loading")',
    :method =>:get,
    :url=>{:controller=>"solicitud_pre_evaluacion_condicion_acuicultura",
    :action=>:save_especies_solicitadas },
    :with=>"'id=#{i.id}&unidad_produccion_condicion_acuicultura_id=#{@unidad_produccion_condicion_acuicultura.id}'")}", :name => "condicion_especies[]", :id => "tipo_especie_acuicultura_#{i.id}" }  %>

  &nbsp;

  <%= i.nombre %>

  <br>

<%  
} %>

<br/>