<div class="marco-titulo"><div><div>
	Modificando <%= Etiquetas.etiqueta(9) %> Jurídico <span class="titulo_modificar">'<%= @empresa.alias %>'</span>
</div></div></div>

<%= render :partial=>'clientes/cliente_empresa/common/tabs' %>
<div class="marco-contenido2">

	<%= form_title_new %>

	<%= render :partial=>'clientes/cliente_empresa_direccion/common/tabs' %>
	<div class="marco-contenido2">

		<%= error %>
		
		<%= form_remote_tag(
			:loading =>'image_load()',
			:loaded => 'image_unload()',
		  :url => { :action => :save_new, :popup=>params[:popup], :empresa_id => @empresa.id } ) -%>
				<%= render :partial => 'form' %>
				<%= form_buttons_edit :empresa_id =>@empresa.id, :popup=>params[:popup] %>
		<%= end_form_tag -%>

	</div>
</div>