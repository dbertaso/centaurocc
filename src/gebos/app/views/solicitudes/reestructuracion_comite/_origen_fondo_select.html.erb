<%= select(
  'solicitud', 'origen_fondo_id',
  @programa_origen_fondo_select.map{|c| ["#{c.nombre} - #{c.descripcion}", c.id]},
  {:prompt=>I18n.t('Sistema.Body.Vistas.General.seleccionar')},
  { :class=>'select', :onchange=>'inputChange();',:style=>'width:400px' } ) %>
