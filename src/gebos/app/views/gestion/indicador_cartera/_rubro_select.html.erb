
<%= select(
  'rubro_id', '',
 options_from_collection_for_select(@rubro_select, 'id', 'nombre'),
      {:prompt=>I18n.t('Sistema.Body.Vistas.General.seleccione')},
      { :class=>'select',  :onchange=>"#{remote_function(
                    :loading =>'image_load()',
                    :loaded => 'image_unload()',
                    :method => :get,
                    :url=>{:controller=>'indicador_cartera',:action=>:rubro_change },
                    :with=>"'rubro_id='+ this.value")};inputChange();"} ) %> 

