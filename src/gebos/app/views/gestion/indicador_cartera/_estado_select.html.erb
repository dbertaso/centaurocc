

<%= select(
  'estado_id', '',
 options_from_collection_for_select(@estado_select, 'id', 'nombre'),
      {:prompt=>I18n.t('Sistema.Body.Vistas.General.seleccione')},
      { :class=>'select', :onchange=>'inputChange();'} ) %> 
