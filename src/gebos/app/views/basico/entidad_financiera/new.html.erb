<div class="marco-contenido">
  <%= form_title_new %>

  <%= error %>
  <%= message %>

  <%= form_remote_tag(
    :loading =>'image_load()',
    :loaded => 'image_unload()',
    :url => { :action => :save_new, :popup=>params[:popup] } ) do-%>

  <%= render 'form' %>

  <%= form_buttons_edit :popup=>params[:popup] %>

  <% end -%>
</div>
