<div class="marco-contenido">
  <%= form_title_new %>
  <div class="marco-contenido2">

    <%= error %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_new, :id=>@analista_cobranzas.id, :popup=>params[:popup] } ) do %>
    <%= render :partial => 'form' %>
    <%= form_buttons_edit :popup=>params[:popup] %>
    <% end %>
  </div>
</div>


