<tr id="row_<%= @analisis_conclusion.id %>_edit">
  <td class="lista-form" colspan="4">

    <%= form_title_edit @analisis_conclusion.nombre %>

    <%= error "row_#{@analisis_conclusion.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @analisis_conclusion.id } ) do -%>

    <%= render :partial => 'form' %>
    <%= link_to_remote(
      image_tag(I18n.t('Sistema.Body.Imagenes.cancelar'), :title=> I18n.t('Sistema.Body.Vistas.General.cancelar')),
      :method => :get,
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :cancel_edit, :id => @analisis_conclusion.id}) -%>
  </td>
  <td></td>
  <td></td>
  <td></td>
</tr>
</table>
<% end -%>
</td>
</tr>
