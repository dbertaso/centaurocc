<tr id="row_<%= @tipo_sistema_acuicultura.id %>_edit">
	<td class="lista-form" colspan="8">

		<%= form_title_edit @tipo_sistema_acuicultura.nombre %>
	
		<%= error "row_#{@tipo_sistema_acuicultura.id}" %>

		<%= form_remote_tag(
		  :loading =>'image_load()',
      :loaded => 'image_unload()',
		  :url => { :action => :save_edit, :id => @tipo_sistema_acuicultura.id } ) do-%>
					<%= render 'form' %>
					<%= link_to_remote(
		        image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
					  :loading =>'image_load()',
						:loaded => 'image_unload()',
						:method=>:get,
		        :url => { :action => :cancel_edit, :id => @tipo_sistema_acuicultura.id}) -%>
			   </td>
		    </tr>
		  </table>
		<% end -%>
</div>

