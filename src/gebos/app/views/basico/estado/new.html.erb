<%= form_title_new %>		
<%= render :partial=>'basico/estado/common/tabs' %>
<div class="marco-contenido2">

	<%= error %>

	<%= form_remote_tag(
		:loading =>'image_load()',
		:loaded => 'image_unload()',
	  :url => { :action => :save_new } ) do-%>
		<%= render 'form' %>
		<%= form_buttons_edit %>
	<% end -%>

</div>
