<tr id="row_<%= @convertidor.id %>_edit">
  <td class="lista-form" colspan="6">
    <%= form_title_edit "#{@convertidor.origen} #{I18n.t('Sistema.Body.Vistas.General.a')} #{@convertidor.destino}" %>

    <%= error "row_#{@convertidor.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',:loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @convertidor.id } ) do -%>
    <%= render :partial => 'form' %>
    <%= link_to_remote(
      image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),:loading =>'image_load()',:loaded => 'image_unload()',
      :method=>:get, :url => { :action => :cancel_edit, :id => @convertidor.id})
    -%>
  </td>
  <td></td>
  <td></td>
  <td></td>
</tr>
</table>
<% end -%>
</td>
</tr>