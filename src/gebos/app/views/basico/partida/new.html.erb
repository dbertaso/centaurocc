<%= form_title_new %>
<%= render 'basico/sector/common/tabs' %>
<div class="marco-contenido2">

  <%= error %>
  <%= message %>

  <%= form_remote_tag(
    :loading =>'image_load()',
    :loaded => 'image_unload()',
    :url => { :action => :save_new, :actividad_id => @actividad.id, :popup=>params[:popup] } ) do -%>
  <%= render  'form' %>
  <%= form_buttons_edit :actividad_id => @actividad.id, :popup=>params[:popup] %>

  <% end -%>

</div>
