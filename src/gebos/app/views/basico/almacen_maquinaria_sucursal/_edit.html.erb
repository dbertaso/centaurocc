<tr id="row_<%= @almacen_maquinaria_sucursal.id %>_edit">

  <td class="lista-form" colspan="6">
    <%= form_title_edit @almacen_maquinaria_sucursal.nombre %>

    <%= error "row_#{@almacen_maquinaria_sucursal.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id=>@almacen_maquinaria_sucursal.id, :almacen_maquinaria_id => @almacen_maquinaria.id } ) do -%>
    <%= render :partial => 'form' %>
    <%= link_to_remote(
      image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :method =>:get,
      :url => { :action => :cancel_edit, :id=>@almacen_maquinaria_sucursal.id}) -%>
  </td>
</tr>
</table>
<% end -%>
</td>

</tr>