<tr id="row_<%= @tipo_iniciativa.id %>_edit">
  <td class="lista-form" colspan="2">

    <%= form_title_edit @tipo_iniciativa.nombre %>

    <%= error "row_#{@tipo_iniciativa.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @tipo_iniciativa.id } ) do -%>

    <%= render :partial => 'form' %>
    <%= link_to_remote(image_tag(I18n.t('Sistema.Body.Imagenes.cancelar'), :title=>I18n.t('Sistema.Body.Vistas.General.cancelar')),
      :method => :get,
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :cancel_edit, :id => @tipo_iniciativa.id}) -%>
  </td>
  <td></td>
  <td></td>
  <td></td>
</tr>
</table>
<% end-%>
</td>
</tr>