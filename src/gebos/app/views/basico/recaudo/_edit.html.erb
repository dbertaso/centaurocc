<tr id="row_<%= @recaudo.id %>_edit">
  <td class="lista-form" colspan="8">

    <%= form_title_edit @recaudo.nombre %>

    <%= error "row_#{@recaudo.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @recaudo.id } ) do -%>
    <%= render :partial => 'form' %>
    <%= link_to_remote(
      image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :method =>:get,
      :url => { :action => :cancel_edit, :id => @recaudo.id}) -%>
  </td>
</tr>
</table>
<% end -%>
</td>
</tr>