<tr id="row_<%= @ciclo_productivo.id %>_edit">
	<td class="lista-form" colspan="6">
	
		<%= form_title_edit @ciclo_productivo.nombre %>
	
		<%= error "row_#{@ciclo_productivo.id}" %>
		
		<%= form_remote_tag(
		  :loading =>'image_load()',
			:loaded => 'image_unload()',
		  :url => { :action => :save_edit, :id => @ciclo_productivo.id } ) do-%>
	
					<%= render 'form' %>
					<%= link_to_remote(image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
					  :loading =>'image_load()',
						:loaded => 'image_unload()',
						:method=>:get,
		        :url => { :action => :cancel_edit, :id => @ciclo_productivo.id}) -%>
			   </td>
			
		    </tr>
		  </table>
		<% end -%>
	</td>
</tr>
