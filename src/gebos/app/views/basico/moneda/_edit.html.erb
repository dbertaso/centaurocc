<tr id="row_<%= @moneda.id %>_edit">
	<td class="lista-form" colspan="5">
	
		<%= form_title_edit @moneda.nombre %>
	
		<%= error "row_#{@moneda.id}" %>
	
		<%= form_remote_tag(
		  :loading =>'image_load()',
			:loaded => 'image_unload()',
		  :url => { :action => :save_edit, :id => @moneda.id } ) do-%>
					<%= render 'form' -%>
					<%= link_to_remote(
		        image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
		        :method => :get,
					  :loading =>'image_load()',
						:loaded => 'image_unload()',
		        :url => { :action => :cancel_edit, :id => @moneda.id}) -%>
			   </td>
		    </tr>
		  </table>
		<% end -%>
	</td>
</tr>
