<tr id="row_<%= @experiencia.id %>_edit">
	<td class="lista-form" colspan="2">
	
		<%= form_title_edit @experiencia.nombre %>
	
		<%= error "row_#{@experiencia.id}" %>
		
		<%= form_remote_tag(
		  :loading =>'image_load()',
			:loaded => 'image_unload()',
		  :url => { :action => :save_edit, :id => @experiencia.id } ) do-%>
	
					<%= render 'form' %>
					<%= link_to_remote(image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
					  :loading =>'image_load()',
						:loaded => 'image_unload()',
						:method=>:get,
		        :url => { :action => :cancel_edit, :id => @experiencia.id}) -%>
			   </td>
			
		    </tr>
		  </table>
		<% end -%>
	</td>
</tr>
