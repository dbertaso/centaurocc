<tr id="row_<%= @sitio_colocacion.id %>_edit">
  <td class="lista-form" colspan="5">

    <%= form_title_edit @sitio_colocacion.sitio %>

    <%= error "row_#{@sitio_colocacion.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @sitio_colocacion.id } ) do -%>
    <%= render :partial => 'form' %>
    <%= link_to_remote(
      image_tag(I18n.t('Sistema.Body.Imagenes.cancelar'), :title=>I18n.t('Sistema.Body.Vistas.General.cancelar')),
      :method => :get,
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :cancel_edit, :id => @sitio_colocacion.id}) -%>
  </td>
</tr>
</table>
<% end -%>
</td>
</tr>
