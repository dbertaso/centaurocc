<tr id="row_<%= @porcentaje_ganancia.id %>_edit">
  <td class="lista-form" colspan="2">

    <%= form_title_edit @porcentaje_ganancia.nombre %>

    <%= error "row_#{@porcentaje_ganancia.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @porcentaje_ganancia.id } ) do -%>

    <%= render :partial => 'form' %>
    <%= link_to_remote(
      image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :method =>:get,
      :url => { :action => :cancel_edit, :id => @porcentaje_ganancia.id}) -%>
  </td>
  <td></td>
  <td></td>
  <td></td>
</tr>
</table>
<% end -%>
</td>
</tr>