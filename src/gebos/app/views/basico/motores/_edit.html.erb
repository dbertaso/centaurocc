<tr id="row_<%= @motores.id %>_edit">
  <td class="lista-form" colspan="8">

    <%= form_title_edit @motores.modelo_motor %>

    <%= error "row_#{@motores.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id_equipo => @motores.id } ) do -%>
    <%= render :partial => 'form' %>
    <%= link_to_remote(
      image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :method => :get,
      :url => { :action => :cancel_edit, :id => @motores.id}) -%>
  </td>
</tr>
</table>
<% end -%>
</td>
</tr>

