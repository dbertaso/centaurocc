<tr id="row_<%= @tenencia_unidad_produccion.id %>_edit">
  <td class="lista-form" colspan="2">

    <%= form_title_edit @tenencia_unidad_produccion.nombre %>

    <%= error "row_#{@tenencia_unidad_produccion.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @tenencia_unidad_produccion.id } ) do -%>
    <%= render :partial => 'form' %>
    <%= link_to_remote(image_tag(I18n.t('Sistema.Body.Imagenes.cancelar'), :title=>I18n.t('Sistema.Body.Vistas.General.cancelar')),
      :method => :get,
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :cancel_edit, :id => @tenencia_unidad_produccion.id}) -%>
  </td>
  <td></td>
  <td></td>
  <td></td>
</tr>
</table>
<% end -%>
</td>
</tr>
