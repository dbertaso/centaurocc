<tr id="row_<%= @eje.id %>_edit">
	<td class="lista-form" colspan="2">
	
		<%= form_title_edit @eje.nombre %>
	
		<%= error "row_#{@eje.id}" %>
		
		<%= form_remote_tag(
		  :loading =>'image_load()',
			:loaded => 'image_unload()',
		  :url => { :action => :save_edit, :id => @eje.id } ) do-%>
	
					<%= render 'form' %>
					<%= link_to_remote(image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
					  :loading =>'image_load()',
						:loaded => 'image_unload()',
						:method=>:get,
		        :url => { :action => :cancel_edit, :id => @eje.id}) -%>
			   </td>
			
		    </tr>
		  </table>
		<% end -%>
	</td>
</tr>
