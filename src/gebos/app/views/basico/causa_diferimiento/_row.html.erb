<% causa_diferimiento = @causa_diferimiento if causa_diferimiento.nil? %>
<tr class="lista<%= even_odd %>" id="row_<%= causa_diferimiento.id %>">
	<%= render :partial => 'data', :locals => { :causa_diferimiento => causa_diferimiento, :even_odd => even_odd } -%>
</tr>
