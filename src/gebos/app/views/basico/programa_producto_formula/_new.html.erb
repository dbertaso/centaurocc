<%= form_title_new %>

<%= error 'form_new' %>

<%= form_remote_tag(
  :loading =>'image_load()',
  :loaded => 'image_unload()',
  :url => { :action => :save_new, :producto_id => @producto.id, :programa_id => @programa.id } ) do -%>
  <%= render 'form' %>
  <%= link_to_remote(image_tag(image_path(I18n.t('Sistema.Body.Imagenes.cancelar'))),
    :loading =>'image_load()', :loaded => 'image_unload()',
    :url => { :action => :cancel_new, :programa_id => @programa.id}) -%>
  </td>
  </tr>
  </table>
<% end -%>
