<%= form_title_search %>
<br>
<%= form_for :avanzar, :html => {:multipart => true} do |f| %>
  <%= f.file_field :archivo %>
  <%= f.submit "Cargar Archivo" %>
<% end %>

<table style="width: 100%">
  <tr>
    <td style="text-align: right">
      <a target='_blank' href="<%= request.protocol + request.host.to_s + ":" + request.port.to_s %>/documentos/maquinaria/carga_masiva.xls"><%=I18n.t('Sistema.Body.Vistas.General.descargar_archivo')%></a>
      <br/><br/>
    </td>
  </tr>
</table>
<center>

  <div>
    <%= @feedback %>
  </div>
</center>
<br/>
<%= link_to(image_tag(I18n.t('Sistema.Body.Imagenes.cerrar')), :controller=> 'catalogo', :action => 'index', :popup=>params[:popup] ) %>