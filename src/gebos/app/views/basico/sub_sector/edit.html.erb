<%= form_title_edit @sub_sector.nombre %>
<%= render :partial=>'basico/sector/common/tabs' %>
<div class="marco-contenido2">
   <%= error  %>
  <%= message  %>

  <%= form_remote_tag(
    :loading =>'image_load()',
    :loaded => 'image_unload()',
    :url => { :action => :save_edit, :id=>@sub_sector.id} ) do -%>
  <%= render :partial => 'form' %>

  <div id="buttons_edit" style="display: none">
    <%= form_buttons_edit :sector_id => @sector.id, :popup=>params[:popup] %>
  </div>

  <%= link_to( image_tag(I18n.t('Sistema.Body.Imagenes.cerrar')),
    { :action => :index, :sector_id => @sector.id, },
    { :id=>'button_close' } ) -%>

  <% end -%>
</div>
