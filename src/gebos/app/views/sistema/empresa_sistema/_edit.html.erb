<tr id="row_<%= @empresa_sistema.id %>_edit">
  <td class="lista-form" colspan="10">

    <%= form_title_edit @empresa_sistema.nombre %>

    <%= error "row_#{@empresa_sistema.id}" %>

    <%= form_remote_tag(
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :save_edit, :id => @empresa_sistema.id } ) do -%>
    <%= render 'form' %>
    <%= link_to_remote(image_tag(I18n.t('Sistema.Body.Imagenes.cancelar')),
      :method => :get,
      :loading =>'image_load()',
      :loaded => 'image_unload()',
      :url => { :action => :cancel_edit, :id => @empresa_sistema.id}) -%>
  </td>
</tr>
</table>
<% end -%>
</td>
</tr>
