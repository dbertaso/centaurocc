<%= select(
  'oficina', 'municipio_id',
  @municipio_list.map{|m| [m.nombre, m.id]},
  {:prompt=>I18n.t('Sistema.Body.Vistas.General.seleccionar')},
  { :class=>'select', :onchange=>'inputChange();' } ) %>